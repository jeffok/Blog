---
title: docker常用命令
date: 2019-02-05 13:05:00
tags: ["K8s", "Kubernetes", "容器", "Docker"]
categories: ["Docker&k8s"]
render_with_liquid: false
permalink: /posts/2019-02-05-docker常用命令/
---

本文档介绍 docker常用命令 的相关内容。

## 进入容器的正确姿势

```bash
sudo docker ps
sudo docker exec -it 775c7c9ee1e1 /bin/bash

```

## 关闭所有Docker容器

```bash
# 停止所有

docker stop $(docker ps -a -q)

# 强制关闭所有

docker kill $(docker ps -a -q)

```

## 关闭并删除所有容器

```bash
docker kill $(docker ps -a -q) && docker rm $(docker ps -a -q)

```

## 删除镜像

```bash
# 删除一个镜像

docker rmi 8f2e175b3bd1

# 删除所有镜像

docker rmi $(docker images -a -q)

```

## docker映射关系查看

```bash
docker inspect "images-name"
```
