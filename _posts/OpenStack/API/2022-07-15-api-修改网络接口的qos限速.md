---
title: api-修改网络接口的qos限速
date: 2022-07-15 09:01:00
tags: ["云计算", "OpenStack"]
categories: ["OpenStack", "API"]
render_with_liquid: false
permalink: /posts/2022-07-15-api-修改网络接口的qos限速/
---

本文档介绍 api-修改网络接口的qos限速 的相关内容。

```python
    def _update_port_qos(self, port_uuid, qos_policy_uuid):
        '''
        修改网络端口的qos限速
        '''

        url = self.neutron_api_url + 'ports/%s' % port_uuid
        body = {"port": {"qos_policy_id": qos_policy_uuid}}
        res = requests.request("PUT", url, data=json.dumps(body),
                                     headers=self._headers()).json()
        return res

        res = {i['id']: {'vm_name': i['name'], 'vm_url': i['links'][0].get('href')} for i in detail['servers']}

```
